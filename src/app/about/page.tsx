import React from "react";
import styles from "./page.module.css";

type Props = {};

export const metadata = {
  title: "About",
  description: "Welcome to the about section of Sore Zore",
};

const About = (props: Props) => {
  return <div>About Page</div>;
};

export default About;
